README
======

This component is managed by org.simple.access.SimpleHttp

Run this class and browse URL http://localhost:8080/simple

To include this component in a larger application, simply construct an instance SimpleHttp, specifying the
correct port and file system resource root directory.